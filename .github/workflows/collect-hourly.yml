# ⚽ Soccer Brain - 시간별 데이터 수집
# 배당 흐름 추적 + 순위/결과/라인업 동기화
#
# GitHub Actions가 클라우드에서 실행 → PC 꺼져도 데이터 수집 계속

name: ⚽ Hourly Data Collection

on:
  schedule:
    # 매 시간 정각 (UTC) → 한국시간으로도 매 시간
    - cron: "0 * * * *"
  
  # 수동 실행 (테스트/디버그용)
  workflow_dispatch:
    inputs:
      tasks:
        description: "실행할 태스크 (쉼표 구분)"
        required: false
        default: "standings,results,odds,lineups"

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: 🟢 Node.js 설정
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: 📦 의존성 설치
        run: npm ci --ignore-scripts
        env:
          PUPPETEER_SKIP_DOWNLOAD: "true"

      - name: 🔧 Prisma 생성
        run: npx prisma generate

      - name: 🤖 데이터 수집
        run: |
          TASKS="${{ github.event.inputs.tasks || 'standings results odds lineups' }}"
          # 쉼표를 공백으로
          TASKS=$(echo "$TASKS" | tr ',' ' ')
          echo "📋 실행: $TASKS"
          npx tsx scripts/collect.ts $TASKS
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          API_SPORTS_KEY: ${{ secrets.API_SPORTS_KEY }}
