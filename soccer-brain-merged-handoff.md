# Soccer Brain — 통합 프로젝트 현황 & Phase 1 계획
## 2026-02-12 기준 (다음 세션 핸드오프용)

---

# 1. 프로젝트 개요

> **예정 경기의 사전 분석 정보를 가장 잘 제공하는 한국어 스포츠 플랫폼**

- 포지셔닝: LiveScore가 아닌 "경기 분석 정보" 중심
- AI 예측 UI 완전 제거, 순수 데이터 정보 플랫폼으로 전환
- ML 시스템(V1~V9) 전부 폐기, 새로 재설계 예정
- 목표: 2025년 4월 초 출시

---

# 2. 기술 스택

| 레이어 | 기술 |
|--------|------|
| 프론트엔드 | React 18 + TypeScript + Vite + Tailwind + Shadcn UI |
| 라우팅 | wouter |
| 데이터 페칭 | @tanstack/react-query |
| 백엔드 | Express 5 + TypeScript |
| ORM | Prisma |
| DB | Neon PostgreSQL |
| 데이터 수집 | API-Football (Ultra plan), Puppeteer (베트맨 배당) |

---

# 3. 현재 상태 요약

```
ML 시스템: 전부 폐기 (V1~V9 모두)
├─ 모든 모델/학습 파이프라인: 폐기 대상
├─ ML 코드: backup 이동 필요
└─ 새로 설계 필요 (Phase 1b)

DB: 유지하되 정제 필요
├─ 현재: 중복 있고 독립성 부족
├─ 목표: 한 경기당 1개 row (완벽 독립)
└─ 우선순위: 최고!

프론트엔드: v2 리빌드 완료 (2/12)
├─ 새 UI 동작 확인됨
├─ 세부 UI 다듬기 필요
└─ 기존 불필요 파일 정리 필요

백엔드: routes-v2.ts 추가 완료 (2/12)
├─ 기존 routes.ts (admin/데이터수집) 유지
├─ 새 프론트엔드용 API 3개 추가
└─ Prisma 스키마 변경 없음
```

---

# 4. 백엔드 현황

## 4-1. 활성 API 엔드포인트

### 새 프론트엔드용 (routes-v2.ts) — 2/12 신규
```
GET /api/v2/fixtures?date=&league=     ← 홈 경기 목록
GET /api/v2/fixtures/:id/detail        ← 경기 상세 (스탯, H2H, 부상, 배당)
GET /api/v2/highlights?date=           ← 주목할 경기
```

### 기존 API 중 계속 사용 (routes.ts)
```
GET /api/standings?leagueId=           ← 순위 페이지
GET /api/leagues                       ← 리그 목록
```

### Admin/데이터 수집 (routes.ts, admin.tsx 전용)
```
POST /api/admin/sync-fixtures          ← 경기 일정 동기화
POST /api/admin/sync-fixtures-smart    ← 스마트 동기화
POST /api/admin/sync-team-stats        ← 팀 스탯 동기화
POST /api/admin/sync-standings         ← 순위 동기화
POST /api/admin/sync-injuries          ← 부상자 동기화
POST /api/admin/sync-odds              ← 해외배당 동기화
POST /api/admin/sync-betman-odds       ← 국내배당 동기화
POST /api/admin/build-features         ← 피처 스냅샷 빌드
POST /api/admin/sync-weather           ← 날씨 동기화
POST /api/admin/sync-venue-geo         ← 경기장 좌표
POST /api/admin/sync-teams             ← 팀 로고/이름 동기화
... (기타 admin/데이터 수집 다수)
```

## 4-2. DB 스키마 (Prisma, 변경 없음)
주요 모델: League, Team, Fixture, Standing, FixtureOdds, BookmakerOdds, FixtureInjury, FixtureWeather, FixtureFeatureSnapshot, FixtureLineup, Player 등

## 4-3. 데이터 현황
- 25개 활성 리그 (5대리그 + K리그 + J리그 + 유럽컵 등)
- 2020~2025 6시즌 데이터 수집 완료
- 13개 북메이커 배당 추적
- FixtureFeatureSnapshot: xG, 점유율, 폼, H2H, 피로도 등 50+개 피처

## 4-4. DB 문제점 (정제 필요)
```
문제:
- 중복 데이터 있음
- 한 경기가 여러 row로 흩어져 있음 (Fixture + Stats + Feature 등)
- 독립성 부족
- 시간 정보 처리 미흡

목표:
- 한 경기당 정확히 1개 row (완벽 독립)
- 중복 없음
- 정보 제공/학습 모두 가능

우선순위: ⭐⭐⭐ (최고!)
```

---

# 5. 프론트엔드 현황

## 5-1. 현재 활성 페이지 (v2 리빌드 완료)

| 페이지 | 파일 | URL | 상태 |
|--------|------|-----|------|
| 홈 | `pages/home.tsx` | `/` | ✅ 리빌드 완료 |
| 경기 상세 | `pages/match-detail.tsx` | `/match/:id` | ✅ 리빌드 완료 (다듬기 필요) |
| 순위 | `pages/standings.tsx` | `/standings` | ✅ 신규 |
| 설정 | `pages/settings.tsx` | `/settings` | ✅ 신규 |
| 관리자 | `pages/admin.tsx` | `/admin` | ✅ 기존 유지 |

## 5-2. 새로 만든 공유 모듈
- `shared/team-names.ts` — 중앙화된 팀 한글명 + 리그 정의
- `components/bottom-nav.tsx` — 3탭 네비 (홈/순위/설정)
- `styles/utilities.css` — no-scrollbar, safe-area

## 5-3. 네비게이션 구조 (확정)
```
바텀 네비: 🏠 홈 | 📊 순위 | 👤 설정
상단: 종목 선택 (축구만 활성)
화면 깊이: 최대 2단계 (홈 → 경기상세)
```

## 5-4. 아직 남아있는 기존 파일 (정리 대상)
```
삭제 대상 페이지:
  pages/analysis.tsx          ← 구 분석 페이지
  pages/match-analysis.tsx    ← 구 경기 상세 (match-detail.tsx로 교체됨)
  pages/live.tsx              ← 구 라이브 탭
  pages/results.tsx           ← 구 결과 탭
  pages/my.tsx                ← 구 마이 페이지 (settings.tsx로 교체됨)
  pages/prediction.tsx        ← 예측 페이지
  pages/laboratory.tsx        ← 실험실 페이지
  pages/match-list.tsx        ← 구 경기 목록
  pages/schedule.tsx          ← 구 일정
  pages/history.tsx           ← 구 히스토리
  pages/league.tsx            ← 구 리그 페이지 (standings.tsx로 교체됨)

삭제 대상 컴포넌트:
  components/analysis-drawer.tsx
  components/analysis-report.tsx
  components/animated-probability-chart.tsx
  components/core-analysis-card.tsx
  components/insight-cards.tsx
  components/match-card.tsx
  components/odds-movement.tsx
  components/pick-fab.tsx
  components/predicted-score.tsx
  components/probability-gauge-bar.tsx
  components/sport-placeholder.tsx
  components/sport-selector.tsx
  components/team-panel.tsx
  components/team-radar-chart.tsx
  components/weather-panel.tsx
  components/bottom-tab-navigation.tsx  ← bottom-nav.tsx로 교체됨
  components/theme-toggle.tsx           ← 확인 후 삭제 여부 결정

삭제 대상 컨텍스트:
  context/prediction-context.tsx
  contexts/pick-context.tsx
  contexts/sport-context.tsx

삭제 대상 ML/예측 코드:
  models/ 폴더 전체 (xgboost V1~V9)
  scripts/train_xgboost*.py
  scripts/predict_server*.py
  scripts/analyze_*.py
  scripts/backtest_*.py
  scripts/simulate_*.py
  server/v7-analysis.ts
  server/v9-analysis.ts
  server/training-data-manager.ts
  server/mining-scheduler.ts
  training_set.json
  mining_state.json
  data/*.csv (학습용 CSV)
```

---

# 6. 완료한 작업 (2/12)

1. **UX 설계 문서 v1.0 확정** (soccer-brain-design-spec-v1.md)
2. **프론트엔드 전면 리빌드**
   - App.tsx, bottom-nav, home, match-detail, standings, settings
   - team-names.ts 중앙화
   - AI 예측 관련 UI 완전 제거
3. **백엔드 v2 라우트 추가** (routes-v2.ts)
   - /api/v2/fixtures (날짜별 + 리그필터)
   - /api/v2/fixtures/:id/detail (부상, 배당, 스탯, H2H, 요약 통합)
   - /api/v2/highlights (하이라이트 자동 판별)
4. **서버 실행 확인 완료** — 기본 동작 OK

---

# 7. Phase 1 전체 계획 & 진행 상황

```
⓪ Claude Code 세팅                    ← 내일 최우선
①-A 기존 파일 정리 (ML 코드 폐기 포함)  ← 내일 (Claude Code로)
①-B UI 다듬기 (경기 상세 집중)          ← 내일 메인 작업
② DB 분석 & 정제                       ← 추후
③ API 확충                            ← 추후
④ 실데이터 검증 & 배포 준비             ← 추후
⑤ Phase 1b: ML 시스템 재설계 (병행)     ← DB 정제 후
```

## ⓪ Claude Code 세팅 (최우선)
- Claude Code 설치 및 프로젝트 연결
- 이후 모든 코딩 작업은 Claude Code로 진행
- claude.ai는 설계/기획용, Claude Code는 코딩/디버깅용

## ①-A 파일 정리 (Claude Code로 실행)
```
목표: 깨끗한 상태에서 시작

- 위 5-4 삭제 목록 한번에 실행
  ├─ 구 페이지/컴포넌트/컨텍스트 삭제
  ├─ ML 코드 전부 backup 이동 (models/, scripts/, v7/v9-analysis 등)
  ├─ 학습 데이터 파일 정리
  └─ training_set.json, mining_state.json 제거
- import 참조 깨진 곳 자동 확인 + 수정
- 빌드 테스트
- "이 파일들만 사용한다" 확정
```

## ①-B UI 다듬기 (메인 작업, Claude Code로 실행)
```
경기 상세 페이지 집중 개선:
- Above the fold 레이아웃 정밀 조정
- 스탯 비교 바 차트 가독성 개선
- 배당 + 내재확률 표시 방식 개선
- 부상자 섹션 디자인
- H2H 바 차트 비율/색상 다듬기
- 경기 요약 텍스트 스타일링
- 빈 데이터 처리 (배당 없음, 스탯 없음 시 표시)

홈 화면:
- 하이라이트 카드 디자인 다듬기
- 경기 행 간격/정렬 미세조정
- 리그 필터 UX 개선
```

## ② DB 분석 & 정제 (최우선 과제)
```
Step 1: 현재 DB 상태 분석
- 테이블 구조 확인 + 데이터 현황
- 중복 데이터 파악 (어떤 기준으로 중복?)
- 한 경기 데이터가 어떻게 흩어져 있나?
- 이상치/결측치 현황

Step 2: DB 정제 및 가공 (⭐ 가장 중요!)
- 신규 DB 스키마: 한 경기당 1개 row, 완벽 독립성
- 정제 기준 확정 (중복 제거, 정규화, 이상치 처리)
- 정제 파이프라인: 원본 → 검증 → 정규화 → 중복 제거 → 저장 → 최종 검증
- 산출물: Prisma schema, 정제 스크립트, 검증 로직, 통계 리포트
- "이 DB는 신뢰할 수 있다" 확정
```

## ③ API 확충
```
정제된 DB 기반 추가 API:
- GET /api/teams/:id/stats (팀 상세 통계)
- GET /api/teams/:id/recent-form (최근 폼)
- GET /api/matches/:id/h2h (상세 맞대결)
- GET /api/stats/team-rankings (팀 랭킹)
→ 각 API 스펙, 응답 형식, Prisma 쿼리 정의
```

## ④ 실데이터 검증 & 배포 준비
```
- 다양한 리그/경기로 테스트
- 엣지 케이스: K리그(시즌 시작 전), 컵대회, 데이터 없는 경기
- 프로덕션 빌드 테스트
- PWA 기본 설정
```

## ⑤ Phase 1b: ML 시스템 재설계 (병행)
```
⚠️ 이전 V1~V9 코드 참고하지 않음
⚠️ 성공 보장 없음 (새로 시도)
⚠️ DB 정제 완료 후 본격 시작

- 학습 목표 재정의 (뭘 예측? 어떤 피처?)
- 모델 설계 (알고리즘 선택, 하이퍼파라미터)
- 검증 전략 (학습/테스트 분할, Cross-validation)
```

---

# 8. 핵심 설계 원칙 (항상 참고)

1. 경기 목록은 미니멀 (팀명 + 시간 + 리그만)
2. 경기 상세는 풍부하게 (단일 스크롤, 탭 없음)
3. 데이터 없으면 섹션 숨김 (빈 공간 금지)
4. W=녹색, D=노란색, L=빨간색
5. 모바일 퍼스트 430px
6. 다크 모드 기본 (#0A0E17 배경)
7. 한국어 네이티브 (모든 UI 한국어)

---

# 9. 우선순위 정리

```
⭐⭐⭐ PRIMARY (필수):
  ├─ 파일 정리 + ML 코드 폐기
  ├─ UI 다듬기 (경기 상세 집중)
  ├─ DB 분석 & 정제
  ├─ API 확충
  └─ 배포 준비

⭐⭐ PARALLEL (병행):
  └─ ML 시스템 재설계 (DB 정제 완료 후)

❌ ABANDONED (폐기 확정):
  └─ V1~V9 모든 ML 코드/모델
```

---

# 10. 참고 파일

- `soccer-brain-design-spec-v1.md` — 전체 UI/UX 설계 문서
- `INTEGRATION_GUIDE.md` — v2 프론트엔드 통합 가이드
- `soccer-brain-phase1-FINAL-v3.md` — Phase 1 원본 전략 문서

---

# 11. 내일 첫 작업 순서

```
1. 이 문서 + 현재 프로젝트 파일 업로드
2. Claude Code 설치 안내 받기
3. Claude Code로 파일 정리 실행 (①-A)
4. Claude Code로 UI 다듬기 시작 (①-B, 경기 상세 집중)
```
